<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/online_test"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizWiz"
        tools:targetApi="31">
        <activity
            android:name=".activities.about_Activity"
            android:exported="false" />
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".activities.question_activity"
            android:exported="true"
            android:theme="@style/Base.Theme.QuizWiz.ActionBar" />
        <activity
            android:name=".activities.splash_screen"
            android:exported="true" />
        <activity
            android:name=".activities.signup_activity"
            android:exported="true" />
        <activity
            android:name=".activities.login_activity"
            android:exported="true" />
        <activity
            android:name=".activities.intro_activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>